def allStrings(string):
    number = len(string) ** 2
    start = 0
    end = 0
    string = "".split(string)
    while start < len(string):
        for i in range(0, len(string)):
            for j in range(0, len(string)):
                
allStrings(0)